
const { Router } = require('express');
// const { Stats } = require('../data/stats');

const router = Router();
  // var statsData;

  // Stats.find((error, stats) => {
  //   if(error) console.log(error);
  //   else statsData = stats;
  // })

  // router.get('/', async(req, res) => {
  //   const data = await Stats.find();
  //   res.send(data);
  // });
  
 
  module.exports = router;